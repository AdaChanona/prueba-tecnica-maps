{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": ".", // Asegura que la raíz es el directorio del proyecto
    "paths": {
      "@/*": ["src/*"] // Configura correctamente el alias para 'src'
    }
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    ".expo/types/**/*.ts",
    "expo-env.d.ts"
  ]
}
